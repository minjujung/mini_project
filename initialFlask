# create virtual environment
virtualenv env

# create directories
mkdir -p templates
mkdir -p static

# create files
touch app.py
cd static
touch style.css
touch script.js
cd ../templates
touch index.html

# run source env/bin/activate
# entering venv 자신의 가상환경명을 env에 적어주기 
# 잘 안되는거 같아서 그냥 runenv로 돌리기
# . ./env/bin/activate
